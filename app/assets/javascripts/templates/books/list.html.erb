<div class="container">
  <h1 class="page-header">Books</h1>

  <form class="navbar-form " role="search">
    <div class="form-group">
      <input type="text" class="form-control" placeholder="Search">
    </div>
    <button type="submit" class="btn btn-default">go</button>
  </form>

  <div class="table-responsive book-list">
    <table class="table table-striped table-hover">
      <thead>
        <tr>
          <th>#</th>
          <th>Name</th>
          <th>Author</th>
          <th>Words Count</th>
          <th>Category</th>
        </tr>
      </thead>
      <tbody>
        <tr ng-show="!editMode" dir-paginate="book in books | itemsPerPage: perPage: 'books'" pagination-id="books" total-items="totalItem">
        <!--<tr ng-repeat="book in books" ng-click="show($index)" ng-show="!editMode">-->
          <th>{{book.index}}</th>
          <th>{{book.name}}</th>
          <th>{{book.author}}</th>
          <th>{{book.word_count | number}}</th>
          <th>{{book.category | capitalize}}</th>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="row">
    <div class="col-md-4"></div>
      <div class="col-md-4">
        <dir-pagination-controls template-url="dirPagination.html" pagination-id="books" on-page-change="pageChanged(newPageNumber)"></dir-pagination-controls>
      </div>
      <div class="col-md-4"></div>
</div>
<!--   <div class="panel panel-default">
    <div class="panel-heading">Panel heading without title</div>
    <div class="panel-body">
      <div class="main-content">
        <i class="fa fa-2x" ng-click="editMode = !editMode" ng-class="editMode ? 'fa-unlock' : 'fa-lock'"></i>
        <table class="table table-hover book-list-table">
          <thead>
            <colgroup>
              <col span="1"/>
              <col span="1"/>
              <col span="1"/>
              <col span="1"/>
              <col class="action-col" span="1"/>
            </colgroup>
            <tr>
              <th>#Name</th>
              <th>Author</th>
              <th>Word Count</th>
              <th>Category</th>
              <th ng-show="editMode || bookForm">Action</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <th>Searching</th>
            </tr>
            <tr ng-repeat="book in books" ng-click="show($index)" ng-show="!editMode">
              <th scope="row">{{book.name}}</th>
              <th>{{book.author}}</th>
              <th>{{book.word_count | number}}</th>
              <th>{{book.category | capitalize}}</th>
            </tr>
            <tr ng-repeat="book in books" ng-show="editMode">
              <th scope="row" ng-blur="editBook($index)" contenteditable ng-model="book.name"></th>
              <th ng-blur="editBook($index)" contenteditable ng-model="book.author"></th>
              <th ng-blur="editBook($index)" contenteditable ng-model="book.word_count"></th>
              <th ng-blur="editBook($index)" contenteditable ng-model="book.category"></th>
              <th>
                <div class="btn-group btn-group-xs action-buttons" role="group" ng-show="editMode">
                  <button type="button" class="btn btn-danger" ng-click="delete($index)">
                    <i class="fa fa-trash-o fa-lg"></i> Delete
                  </button>
                  <button type="button" class="btn btn-info" ng-click="manageUpload($index)">
                    <i class="fa fa-upload fa-lg"></i>Upload
                  </button>
                </div>
              </th>
            </tr>
            <tr ng-show="bookForm &&  editMode" class="new-book-form">
              <th><input type="text" placeholder="Name" ng-model="book.name"></th>
              <th><input type="text" placeholder="Author" ng-model="book.author"></th>
              <th><input type="text" placeholder="Word count" ng-model="book.word_count"></th>
              <th><input type="text" placeholder="Type" ng-model="book.category"></th>
              <th>
                <div class="btn-group action-buttons" role="group">
                  <button type="button" class="btn btn-info" ng-click="addBook()">Create</button>
                  <button type="button" class="btn btn-danger" ng-click="cancel()">Cancel</button>
                </div>
              </th>
            </tr>
            <tr ng-click="bookForm = !bookForm" class="disable-hover">
              <th><img ng-show="editMode" src="./right-arrow.gif" /></th>
              <th></th>
              <th></th>
              <th></th>
              <th ng-show="editMode || bookForm"></th>
            </tr>
          </tbody>
        </table>
        </div>
      </div>
    </div>
  </div> -->
</div>

